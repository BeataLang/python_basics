Возникли трудности с выполнением второго задания.
Несмотря на то, что я постаралась подойти к нему с нескольких сторон, единственный работающий кусок кода выглядит так:

main_list = [i ** 3 for i in range(1, 1001, 2)

for num in main_list:
    c = num % 10
    num = num // 10
    b = num % 10
    a = num // 10
    print(a + b + c)

Таким образом я:
1. Создаю список кубов нечетных чисел.
2. Вывожу сумму цифр каждого из них.

Проблема в том, что мне не удается превратить полученные в цикле for значения в полноценный список, чтобы можно было работать дальше.

Я предполагаю, что дальше возникает нечто похожее:

if ??? % 7 == 0:
    sum(???)
if (??? + 17) % 7 == 0:
    sum(???)

Или перебор элементов списка по индексу через цикл while.

Но процесс перевода значений в список остается мне непонятным.